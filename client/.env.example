# Copy this file to .env and fill in the values below.
# .env is gitignored — never commit real values.

# Base URL of the deployed backend API (optional in local dev — Vite
# proxies /api to http://localhost:5000 automatically, see vite.config.js).
# REQUIRED in production if the frontend and backend are deployed
# separately (e.g. frontend on Vercel, backend on Render) — set this to
# the backend's full URL, e.g. https://your-app.onrender.com/api. Leave
# blank only if the backend is served from the same origin as the
# frontend.
VITE_API_URL=

# Google Sheets booking log (optional). See
# docs/google-sheets/BookingSheetSync.gs for the one-time setup — deploy
# that Apps Script as a Web App, then paste its /exec URL and the shared
# secret you chose in it here. Leave both blank to skip Sheets logging
# entirely; the booking form still works fine via Web3Forms without them.
VITE_SHEETS_WEBHOOK_URL=
VITE_SHEETS_SECRET=
